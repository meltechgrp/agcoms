model Posts {
  id        String   @id @default(cuid())
  title     String
  content   String
  slug      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  images    Image[]
}
